<%= bootstrap_form_for(@project) do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :notes %>
  <%= f.datetime_field :date %>
  <%= f.collection_radio_buttons :category, Project.categories, :first, :first  %>
  <%= f.submit "Save Event" %>
<% end %>
